@layout('layout.app')

@section('content')
  <h1>Tasks</h1>
  <a href="{{ route('tasks.create') }}">Nova Tarefa</a>
  <ul>
    @each(task in tasks)
      <li>
        <a href="{{ route('tasks.show', { id: task.id }) }}">
          {{ task.title }}
        </a>
      </li>
    @endeach
  </ul>
@endsection
